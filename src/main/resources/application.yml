server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jsorathiya/cloud-configuration
          clone-on-start: true
          default-label: main